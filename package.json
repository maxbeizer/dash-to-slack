{
  "name": "dash-to-slack",
  "version": "1.0.0",
  "description": "Send Amazon Dash button presses to Slack",
  "main": "./lib/dash-to-slack.js",
  "scripts": {
    "build": "babel src --presets babel-preset-es2015 --out-dir lib",
    "prepublish": "npm run build",
    "test": "standard"
  },
  "repository": {
    "type": "git",
    "url": "github.com/maxbeizer/dash-to-slack"
  },
  "author": "Max Beizer <max.beizer@gmail.com> (http://maxbeizer.com)",
  "license": "MIT",
  "dependencies": {
    "node-dash-button": "^0.6.1",
    "slack-notify": "^0.1.6"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-es2015": "^6.22.0"
  },
  "standard": {
    "ignore": [
      "lib/"
    ]
  }
}
